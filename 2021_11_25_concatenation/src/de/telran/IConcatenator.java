package de.telran;

public interface IConcatenator {

    /**
     * The method must return the concatenation of all the strings from the array
     * @param strs - an array of strings to concatenate
     * @return concatenation of the strs
     */
    String concat(String[] strs);
}
